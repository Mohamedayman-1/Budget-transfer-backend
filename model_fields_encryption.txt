# Budget Transfer Backend - Model Fields Encryption Status

## Account and Entitys App

### XX_Account
Encrypted Fields:
- parent (EncryptedCharField)
- alias_default (EncryptedCharField)

Normal Fields:
- account (CharField)

### XX_Entity
Encrypted Fields:
- parent (EncryptedCharField)
- alias_default (EncryptedCharField)

Normal Fields:
- entity (CharField)

### XX_PivotFund
Encrypted Fields:
- actual (EncryptedCharField)
- fund (EncryptedCharField)
- budget (EncryptedCharField)
- encumbrance (EncryptedCharField)

Normal Fields:
- entity (IntegerField)
- account (IntegerField)
- year (IntegerField)

### XX_ACCOUNT_ENTITY_LIMIT
Encrypted Fields:
- is_transer_allowed_for_source (EncryptedBooleanField)
- is_transer_allowed_for_target (EncryptedBooleanField)
- is_transer_allowed (EncryptedBooleanField)
- source_count (EncryptedIntegerField)
- target_count (EncryptedIntegerField)

Normal Fields:
- id (AutoField)
- account_id (CharField)
- entity_id (CharField)

## Budget Management App

### xx_BudgetTransfer
Encrypted Fields:
- transaction_date (EncryptedCharField)
- amount (EncryptedCharField)
- status (EncryptedCharField)
- requested_by (EncryptedCharField)
- notes (EncryptedCharField)
- gl_posting_status (EncryptedCharField)
- approvel_1 through approvel_4 (EncryptedCharField)
- approvel_1_date through approvel_4_date (EncryptedDateTimeField)
- attachment (EncryptedCharField)
- fy (EncryptedIntegerField)
- report (EncryptedCharField)
- type (EncryptedCharField)

Normal Fields:
- transaction_id (AutoField)
- user_id (IntegerField)
- request_date (DateTimeField)
- code (CharField)
- status_level (IntegerField)
- group_id (IntegerField)
- interface_id (IntegerField)
- reject_group_id (IntegerField)
- reject_interface_id (IntegerField)
- approve_group_id (IntegerField)
- approve_interface_id (IntegerField)

### xx_BudgetTransferAttachment
Encrypted Fields:
- file_name (EncryptedCharField)
- file_type (EncryptedCharField)

Normal Fields:
- attachment_id (AutoField)
- file_size (IntegerField)
- file_data (BinaryField)
- upload_date (DateTimeField)
- budget_transfer (ForeignKey)

### xx_BudgetTransferRejectReason
Encrypted Fields:
- reason_text (EncryptedCharField)
- reject_date (EncryptedDateTimeField)
- reject_by (EncryptedCharField)

Normal Fields:
- id (AutoField)
- Transcation_id (ForeignKey)

## ADJD Transaction App

### xx_TransactionTransfer
Encrypted Fields:
- account_name (EncryptedTextField)
- approved_budget (EncryptedTextField)
- available_budget (EncryptedTextField)
- reason (EncryptedTextField)
- cost_center_name (EncryptedTextField)
- encumbrance (EncryptedTextField)
- actual (EncryptedTextField)

Normal Fields:
- transfer_id (AutoField)
- cost_center_code (IntegerField)
- from_center (TextField)
- to_center (TextField)
- transaction (ForeignKey)
- account_code (IntegerField)
- done (IntegerField)
- file (FileField)

## User Management App

### xx_UserLevel
Encrypted Fields:
- description (EncryptedTextField)

Normal Fields:
- name (CharField)
- level_order (PositiveIntegerField)

### xx_notification
Encrypted Fields:
- message (EncryptedTextField)

Normal Fields:
- user (ForeignKey)
- is_read (BooleanField)
- created_at (DateTimeField)
- is_system_read (BooleanField)
- is_shown (BooleanField)

## Admin Panel App

### MainCurrency
All Normal Fields:
- id (AutoField)
- name (CharField)
- icon (CharField)

### MainRoutesName
All Normal Fields:
- id (AutoField)
- english_name (CharField)
- arabic_name (CharField)
